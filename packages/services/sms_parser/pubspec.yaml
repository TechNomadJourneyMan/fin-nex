name: fnx_sms_parser
description: Pocket Flow SMS/Push notification parsing for Kazakhstan banks (PRD F-03).
publish_to: none
version: 0.1.0

environment:
  sdk: ^3.5.0

dependencies:
  crypto: ^3.0.5
  meta: ^1.15.0

dev_dependencies:
  test: ^1.25.7
  lints: ^4.0.0
